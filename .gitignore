# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Model artifacts — too big for git, EXCEPT the final adapter (via git-lfs):
# it is the one irreplaceable training output; everything else (merged f16,
# GGUF quants) rebuilds from it + public base models.
models/*
!models/adapter/
models/adapter/*
!models/adapter/final/
outputs/
runs/
*.gguf
*.safetensors
*.bin
!models/adapter/final/adapter_model.safetensors

# Harvested/derived data (rebuilt by scripts/01–04 from pinned refs)
data/raw/
data/processed/

# Local tools & secrets
llama.cpp-bin/
llama.cpp-src/
unsloth_compiled_cache/
.env
packaging/Modelfile

# Generated by demo Build mode / agent runs
workspace/
